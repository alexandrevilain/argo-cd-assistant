{
  "name": "backend",
  "version": "0.0.1",
  "scripts": {
    "dev": "NODE_TLS_REJECT_UNAUTHORIZED=0 LOG_FORMAT=pretty bun run --hot src/index.ts",
    "build": "bun build --compile --minify --sourcemap src/index.ts --outfile dist/server"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "catalog:",
    "@hono/structured-logger": "^0.1.0",
    "ai": "catalog:",
    "argocd": "workspace:*",
    "hono": "^4.12.27",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "catalog:"
  }
}
